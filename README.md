
  # mmm

  This is a code bundle for mmm. The original project is available at https://www.figma.com/design/eCudZhXHeHIX8GwaydNdzp/mmm.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  